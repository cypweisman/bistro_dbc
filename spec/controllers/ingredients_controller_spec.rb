require "rails_helper"

describe IngredientsController do

  describe "POST #create" do
    context 'when valid params are passed' do

      # it "assigns the newly created ingredient as @ingredient" do
      #   post :create, { ingredient: {username: "jack_noble", password: "password", email: "jack@email.com"} }
      #   new_user = User.last

      #   expect(assigns(:ingredient)).to eq recipe
      # end

    #   it "redirects to the users show page" do
    #     post :create, { user: {username: "jack_noble", password: "password", email: "jack@email.com"} }
    #     new_user = User.last

    #     expect(response).to redirect_to "/users/#{new_user.id}"
    #   end
    # end
    end
  end
end
